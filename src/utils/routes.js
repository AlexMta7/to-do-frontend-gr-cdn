const routesBackend = Object.freeze({
  url: 'http://localhost:3000',
  // url: 'https://todo-backend-gr-cdn.onrender.com',
})

export const routes = Object.freeze({
  home: '/',
  backend: routesBackend,
})
